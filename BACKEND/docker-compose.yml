services:
  backend:
    build: .
    container_name: finshield-backend
    ports:
      - "5000:5000"
    working_dir: /app
    command: npm start
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
